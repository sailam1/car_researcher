For each vehicle in the shortlist, explain why it fits this user and list 2–3 pros and 1–2 cons based on specs and customer feedback.

User context:
{narrative_summary}

Vehicles JSON:
{vehicles_json}

Return JSON array: [{ "vehicle_id": "...", "reason": "...", "pros": ["..."], "cons": ["..."] }]
